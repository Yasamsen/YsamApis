{
  "name": "samapi",
  "version": "1.0.0",
  "private": true,
  "description": "Modern serverless API documentation website for Vercel",
  "scripts": {
    "dev": "node dev-server.js",
    "build": "node scripts/generate-manifest.js",
    "start": "node dev-server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
